version: 3

project:
  locator: SolaceProducts_solace-quarkus
  id: SolaceProducts_solace-quarkus
  name: solace-quarkus
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
